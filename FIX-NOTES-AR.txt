تحديث DP-900 DUMP - إصلاح مفتاح الإجابات

تم إصلاح مشكلة ظهور الإجابة الصحيحة كإجابة خاطئة في DUMP المكوّن من 82 سؤالاً.

الإصلاحات الأساسية:
1) dump-56: توحيد الإجابة مع نص الاختيار "Pie charts".
2) dump-67: توحيد الإجابة مع نص الاختيار المعروض بالكامل.
3) dump-79: توحيد الإجابة مع الاختيار "semi-structured".
4) إضافة فحص تلقائي يمنع بناء الموقع إذا كانت أي إجابة صحيحة غير موجودة ضمن الاختيارات.
5) إضافة إعادة تصحيح تلقائية للإجابات القديمة المحفوظة في المتصفح، حتى تتحول نتيجة السؤال 79 من خطأ إلى صحيح من غير حذف التقدم.

لرفع التحديث من داخل فولدر المشروع المرتبط بـ GitHub:
  git add .
  git commit -m "Fix DP900 dump answer grading"
  git push
